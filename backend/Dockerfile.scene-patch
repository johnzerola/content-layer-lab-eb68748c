# Apply application-only fixes to a validated, locally available runtime.
# Does not download dependencies, models or change the inherited entrypoint.
ARG BASE_IMAGE
FROM ${BASE_IMAGE}
COPY --chown=10001:10001 app/ /app/app/
COPY --chown=10001:10001 runpod_handler.py /app/runpod_handler.py
