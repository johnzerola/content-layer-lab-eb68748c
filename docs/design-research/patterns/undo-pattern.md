# Undo pattern

Pattern: each meaningful command has a clear name, immediate result and reversible history. Keyboard and visible controls agree. Current gap: caption, timeline and canvas mutations need a common transaction boundary. Experiment: edit a caption, split a clip, undo/redo and verify preview/export mapping.
